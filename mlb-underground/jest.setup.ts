// jest-dom adds DOM matchers (toBeInTheDocument, toHaveStyle, ...).
import '@testing-library/jest-dom';

// Keep test output readable — the app logs a fair amount at info level.
// eslint-disable-next-line no-console
console.log = jest.fn();
