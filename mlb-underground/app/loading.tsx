// Shown instantly (from the client Router Cache) while a route's server
// component streams in, so navigation gives immediate feedback instead of
// appearing to hang on the previous page.
export default function Loading() {
  return (
    <div></div>
  );
}
